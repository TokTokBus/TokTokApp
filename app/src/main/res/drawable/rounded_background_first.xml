<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" />
    <corners android:radius="20dp" /> <!-- 이 부분에서 테두리를 둥글게 설정 -->
    <size
        android:height="324dp"
        android:width="320dp"/>
</shape>
